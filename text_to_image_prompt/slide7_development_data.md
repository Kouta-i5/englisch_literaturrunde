Create only the central figure or diagram for a Japanese medical AI PowerPoint presentation.

Style:
Create a clean scientific schematic figure similar to a research paper figure, adapted for PowerPoint.
Use a pure white background.
Use mostly black, dark gray, and thin gray lines.
Most boxes should have white fill with black or dark gray outlines.
Use gray or black arrows.
Use dark navy only for section labels or key headings.
Use red only for “EyeDiff” if shown as a small destination block on the right edge.
Do not use blue or pale blue filled backgrounds.
Avoid cartoon style and decorative elements.
Do not create a full presentation slide.
Do not add a slide title.
Leave enough empty margin around the figure.
Use 16:9 aspect ratio.

Typography:
Japanese labels: MS PGothic style.
English / numbers: Arial style.

Medical image thumbnail style:
CFP thumbnails in orange-red.
OCT and FFA thumbnails in grayscale.
Do not create patient-identifiable images.

Accuracy:
Use only the exact values provided below.

---

Create only the LEFT-PANEL figure for Slide 7:
Generative model development data (EyeDiff training data).

Figure theme:
A single self-contained panel explaining what data are used to TRAIN EyeDiff.

Layout:
One large white outlined rectangle, landscape orientation.

Panel header (dark navy, top):
生成モデル開発用データ

Main statistics (large, centered):
8 datasets
42,048 images
14 modalities
80+ disease categories

Subtitle (gray):
大規模・多モーダル・多疾患の画像–テキストペア

Visual elements:
- Left side: stacked document + image pairs (image thumbnail + short text line)
- Center: row of modality thumbnails with labels:
  CFP, OCT, FFA, ICGA, FAF, Slit Lamp
- Bottom: a simple horizontal list of 8 small dataset tags:
  Retinal Image Bank
  EyePACS
  OCTDL
  REFUGE
  ORIGA
  RIM-ONE
  DRISHTI
  GAMMA

Optional right-edge exit:
A thick gray arrow leaving the panel to the right.
Arrow label:
学習用データ → EyeDiff

Small destination hint (optional, minimal):
Tiny box labeled EyeDiff (red text only for EyeDiff).

Do NOT include downstream evaluation datasets.
Do NOT include downstream task names.
Do NOT show arrows from evaluation data.

Use exact numbers only:
8, 42,048, 14, 80+.
